{
  "name": "bmarwell-website",
  "version": "1.0.0",
  "description": "Personal website for Benjamin Marwell with optimized build pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "bun run clean",
    "build": "bun run build:html && bun run build:fonts && bun run build:avatar && bun run build:blog && bun run build:favicon && bun run build:compress",
    "build:html": "node scripts/build-html.js && node scripts/generate-sitemap.js",
    "build:fonts": "node scripts/copy-fonts.js",
    "build:avatar": "node scripts/download-avatar.js",
    "build:favicon": "node scripts/generate-favicon.js",
    "build:compress": "bun run compress:zstd && bun run compress:brotli && bun run compress:gzip",
    "build:blog": "node scripts/download-blog-assets.js",
    "compress:zstd": "node scripts/compress-zstd.js",
    "compress:brotli": "node scripts/compress-brotli.js",
    "compress:gzip": "node scripts/compress-gzip.js",
    "serve": "http-server dist -p 8080 -c-1 --brotli --gzip",
    "preview": "bun run build && bun run serve",
    "test": "bun run test:build && vitest run",
    "test:build": "ZSTD_LEVEL=3 BROTLI_LEVEL=4 GZIP_LEVEL=6 bun run build"
  },
  "devDependencies": {
    "@fontsource/roboto": "^5.2.9",
    "@mongodb-js/zstd": "^7.0.0",
    "@vitest/ui": "^4.0.16",
    "brotli": "^1.3.3",
    "firacode": "^6.2.0",
    "google-fonts-helper": "^3.7.3",
    "html-minifier-terser": "^7.2.0",
    "http-server": "^14.1.1",
    "imagemin": "^9.0.1",
    "imagemin-mozjpeg": "^10.0.0",
    "imagemin-optipng": "^8.0.0",
    "node-fetch": "^3.3.2",
    "opentype.js": "^1.3.4",
    "pako": "^2.1.0",
    "rimraf": "^6.0.1",
    "serve-handler": "^6.1.5",
    "sharp": "^0.34.5",
    "vitest": "^4.0.16"
  },
  "trustedDependencies": [
    "@mongodb-js/zstd",
    "mozjpeg",
    "optipng-bin"
  ],
  "keywords": [
    "personal-website",
    "portfolio",
    "static-site"
  ],
  "author": "Benjamin Marwell",
  "license": "SEE LICENSE IN LICENSE-APACHE-2.0 OR LICENSE-EUPL-1.2"
}
